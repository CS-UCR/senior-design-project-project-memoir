query getUserByID ($id: ID!){
    getUser(id: $id) {
        id
        username
        create_date
        status
        profile_icon
    }
}
mutation createUser ($userInput: CreateUserInput!){
    createUser(input: $userInput) {
        id
        username
        create_date
    }
}
